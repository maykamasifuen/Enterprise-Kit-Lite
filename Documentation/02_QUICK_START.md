### Sidebar height looks cut off

Fixed in v5.1 — `app-root` is now a flex column, `app-shell` uses `flex: 1` instead of `height: 100dvh`. The sidebar fills the full viewport height correctly.

---

*← [Overview](./01_OVERVIEW.md) · [User Guide →](./03_USER_GUIDE.md)*  
*v5.2.0 · Updated March 2, 2026*
